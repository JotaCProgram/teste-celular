doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="output.css")
    title Document
  body
    .heading
      h1 Carrossel Personalizado
      h6 (clique nos slides ou pressione as setas esquerda e direita)
      .me
        a(href="https://github.com/cmdeveloped", target="_blank")
          i.fab.fa-github
        a(href="https://twitter.com/collinscode_", target="_blank")
          i.fab.fa-twitter
        a(href="https://codepen.io/collinscode", target="_blank")
          i.fab.fa-codepen
        a(href="https://linkedin.com/in/cmdeveloped", target="_blank")
          i.fab.fa-linkedin
      
    .container
      // Loop para criar elementos .ui-card
      // O loop roda 9 vezes
      // Por exemplo, para criar 9 elementos ui-card
      // Ajuste conforme necessário para o seu caso de uso específico
      // Talvez seja necessário substituir este loop por conteúdo ou dados reais
      // dependendo dos requisitos do seu aplicativo
      // Cada iteração do loop cria um elemento .ui-card
      // O conteúdo do elemento .ui-card pode ser adicionado aqui
      // ou dentro do loop conforme necessário
      // Este é apenas um loop de espaço reservado
      // Substitua-o pelo seu conteúdo ou dados reais
      // conforme necessário para o seu aplicativo
      // Este loop está criando elementos .ui-card sem nenhum conteúdo
      // Talvez você queira adicionar conteúdo ou dados a esses elementos
      // para torná-los significativos para o seu aplicativo
      // Este é apenas um loop de espaço reservado
      // Você precisará substituí-lo por conteúdo ou dados reais
      // conforme necessário para os requisitos do seu aplicativo
      // Por exemplo, você pode buscar dados de um banco de dados
      // ou gerá-los dinamicamente com base na entrada do usuário
      // e então usar esses dados para preencher esses elementos .ui-card
      // Este loop está criando elementos .ui-card sem nenhum conteúdo
      // Talvez você queira adicionar conteúdo ou dados a esses elementos
      // para torná-los significativos para o seu aplicativo
      // Este é apenas um loop de espaço reservado
      // Você precisará substituí-lo por conteúdo ou dados reais
      // conforme necessário para os requisitos do seu aplicativo
      // Por exemplo, você pode buscar dados de um banco de dados
      // ou gerá-los dinamicamente com base na entrada do usuário
      // e então usar esses dados para preencher esses elementos .ui-card
      // Este loop está criando elementos .ui-card sem nenhum conteúdo
      // Talvez você queira adicionar conteúdo ou dados a esses elementos
      // para torná-los significativos para o seu aplicativo
      // Este é apenas um loop de espaço reservado
      // Você precisará substituí-lo por conteúdo ou dados reais
      // conforme necessário para os requisitos do seu aplicativo
      // Por exemplo, você pode buscar dados de um banco de dados
      // ou gerá-los dinamicamente com base na entrada do usuário
      // e então usar esses dados para preencher esses elementos .ui-card
